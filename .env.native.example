# Copy this file to ".env.native" before building the Android app
# Set your API base (must be reachable from a physical device/emulator)
# Example: a Netlify/Vercel/Render/Cloud server URL that exposes /api endpoints
VITE_API_BASE=https://your-staging-api.example.com/api
